'use strict';

describe('aia.splash module', function() {

  beforeEach(module('aia.splash'));

  describe('splash controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SplashCtrl = $controller('SplashCtrl');
      expect(SplashCtrl).toBeDefined();
    }));

  });
});